INCLUDE_DIRECTORIES(.)
INCLUDE_DIRECTORIES(../include)
ADD_LIBRARY(term SHARED fork.c memory.c render.c term.c vt100_escape.c)
TARGET_LINK_LIBRARIES(term util)
INSTALL(TARGETS term LIBRARY DESTINATION lib)
